import React from 'react';
import classes from './elemCommunicationInfo.scss';

const communicationInfo = (props) => {
  return (
    <div className={classes.CommunicationInfo}>COMMUNICATION_INFO</div>
  );
};

export default communicationInfo;
